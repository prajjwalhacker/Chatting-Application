<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Live visitor panel</title>
    <style>
        .container{
            height: 500px;
            width: 600px;
            background-color: aquamarine;
            border: 3px solid blueviolet;
            border-radius: 10px;
        }
        .message {
            background-color: burlywood;
            width: 500px;
            margin: 10px;
            border: 2px solid black;
            border-radius: 6px;
            padding: 5px;
        }
        .left {
            float: left;
            clear: both;
        }
        .live{
            margin-top: 5px;
            margin-bottom: 5px;
            margin-left: 220px;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }
        .navbar{
            background-color: lightblue;
            border: 3px solid black;
            border-radius: 10px;
        }
        .navbar ul{
            overflow: auto;
        }
        .navbar li{
            float : left;
            list-style: none;
        }
        .navbar li a{
            padding : 3px 5px;
            font-family: Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
            text-decoration: none;
        }
        img{
            display: block;
            margin : auto;
            border-radius: 20px;
            margin-top: 10px;
            height: 500px;
            width: 600px;
            border: 4px solid black;
        }
    </style>
</head>
<body>
    <header>
        <nav class="navbar">
            <ul>

                <li><a href = "/chat">Chatting App</a></li>
                <li><a href = "#">Voice Call App</a></li>
                <li><a href = "#">Video Call App</a></li>
                <li><a href = "/live" target="_blank" rel="noopener noreferrer">Live Visitors</a></li>
            </ul>
        </nav>
    </header>
    <div class = "live">Live Clients</div>
    <div class = "container">
         <% for(let val of map.keys()){  %>
            <div class = "message left"><%= users[val] %> : <%= map.get(val) %></div>
          <% }  %>
    </div>
</body>
</html>